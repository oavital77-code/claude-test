# מדריך תפעול — בקליניקה (Cleana)

מסמך הזה מיועד למישהו שלא מכיר את המערכת בכלל — כדי שיוכל להבין על מה מדובר,
איפה כל דבר יושב, ואיך לתחזק את המערכת בלי תלות במי שבנה אותה.

**עדכון המסמך:** בכל פעם שמתגלה משהו קריטי (הגדרה נדרשת, מגבלה, החלטה
תפעולית) — זה נכנס לכאן, בעיקר ל"יומן עדכונים קריטיים" בתחתית הקובץ.

---

## 1. מה המערכת הזו בכלל

**בקליניקה** (שם המותג באפליקציה: **Cleana**) — מערכת לניהול השכרת חדרי
קליניקה. כ-300 מטפלים, 2 סניפים, 10 חדרים. יש שני חלקים:

- **ממשק מטפלים** — קביעת תורים, רכישת כרטיסיות שעות, ססיות שבועיות קבועות,
  תשלומים.
- **פאנל אדמין** — ניהול מטפלים, שיבוץ ידני, אישור ססיות, רישום חריגות,
  דוחות.

האפיון המלא (מה המערכת אמורה לעשות, כללי עסק, תמחור) נמצא ב-`baclinica-spec.md`.
כללי הפיתוח (מה מותר/אסור בקוד) נמצאים ב-`CLAUDE.md`.

---

## 2. שלושת השירותים שהמערכת בנויה עליהם

המערכת **לא** יושבת על שרת אחד — היא מורכבת משלושה שירותי ענן נפרדים,
שכל אחד אחראי על דבר אחר. חשוב להבין את החלוקה כי בעיות תפעוליות כמעט תמיד
קשורות לאחד מהם ספציפית.

### GitHub — איפה הקוד חי
המאגר (repository) `oavital77-code/claude-test`. כל שינוי בקוד נכתב, נבדק
ונדחף (push) לענף `claude/new-session-wgpgpn`. GitHub הוא רק **אחסון קוד** —
הוא לא מריץ את האתר בעצמו.

### Vercel — איפה האתר *רץ*
Vercel לוקח את הקוד מ-GitHub, בונה אותו (build) ומריץ אותו כאתר חי בכתובת
`https://baclinica.vercel.app`. כל דחיפה (push) לענף גורמת ל-deploy אוטומטי
חדש. דברים שיושבים ב-Vercel:
- **Environment Variables** — משתני סביבה (מפתחות API, כתובות) שהאפליקציה
  צריכה כדי לרוץ. **שינוי במשתנה סביבה לא נכנס לתוקף עד שעושים Redeploy.**
- **Cron Jobs** — משימות מתוזמנות (למשל "כל יום ב-03:00 שבץ הזמנות ססיה
  90 יום קדימה"). ⚠️ בתוכנית Vercel Hobby (החינמית) אי אפשר להריץ cron יותר
  מפעם ביום, גם אם מוגדר אחרת — כל ה-cron-ים במערכת מוגדרים להריץ פעם ביום
  בגלל המגבלה הזו.
- הפרויקט ב-Vercel נקרא **"baclinica"**.
- **Region** — `vercel.json` קובע `"regions": ["syd1"]` (סידני). זה **לא**
  שרירותי ואסור למחוק: מסד הנתונים ב-Supabase יושב ב-`ap-southeast-2`, גם הוא
  סידני. ברירת המחדל של Vercel היא `iad1` (וושינגטון) — ואז כל שאילתה למסד
  חוצה את האוקיינוס השקט הלוך ושוב (~200ms לכל קריאה), וכל טעינת עמוד עושה
  כמה כאלה ברצף. הצמדת השרתים למסד מבטלת את הנסיעה הזו.
  אם אי פעם מעבירים את פרויקט ה-Supabase ל-region אחר — חובה לעדכן כאן בהתאם
  (פרנקפורט → `fra1`, לונדון → `lhr1`, וירג'יניה → `iad1`).

### Supabase — איפה כל הנתונים והאימות
Supabase הוא Postgres (מסד נתונים) + מערכת Authentication (התחברות משתמשים)
+ Storage (קבצים, כמו תמונות חדרים) — הכל ביחד. הפרויקט נקרא
**"oavital77-code's Project"**, וה-project ref שלו הוא `ekfgidxqjuysllkqdygl`
(מופיע בכל כתובת URL של הדשבורד). דברים שיושבים ב-Supabase:
- **הטבלאות עצמן** (משתמשים, הזמנות, תשלומים וכו').
- **RPC Functions** — פונקציות ב-SQL (לא ב-Next.js!) שמבצעות את כל הפעולות
  העסקיות הרגישות (קביעת תור, רכישת כרטיסייה וכו'). ר' סעיף 5.
- **Authentication** — ניהול משתמשים, סיסמאות, הגדרות אבטחה (Authentication
  → Sign In / Providers בדשבורד).
- **SQL Editor** — מסך שבו מריצים קוד SQL ישירות מול מסד הנתונים (משמש
  להרצת מיגרציות, ר' סעיף 6).

**חשוב להבין:** Vercel לא "יודע" כלום על Supabase חוץ ממה שכתוב במשתני
הסביבה שלו (כתובת הפרויקט + מפתחות). שינוי בקוד לא משנה שום דבר ב-Supabase —
צריך להריץ שינויים במסד הנתונים בנפרד, ידנית, דרך ה-SQL Editor.

### Resend — שליחת מיילים
שירות שדרכו נשלחים מיילים (כרגע: אישורי הזמנה, התראות תשלום). ⚠️ **מצב נוכחי:
לא מאומת דומיין** — כלומר אפשר לשלוח מיילים רק לכתובת בעלת החשבון
(`oavital77@gmail.com`), לא לאף אחד אחר. ר' סעיף 7 להסבר מלא ולפתרון.

### WooCommerce — סליקת תשלומים
כל תשלום (כרטיסיות, ססיות) עובר דרך חנות ה-Woo (`baclinica.co.il`) —
Cleana לא יוצרת דפי תשלום ולא מדברת עם gateway ישירות (הוחלט לזנוח
אינטגרציית PayPlus ישירה — מעולם לא חוברה בפועל). דורש:
1. Product ID לכל אחד מ-5 מוצרי הכרטיסיות → נכנס ל-`woo_product_tiers`
   (SQL ידני, אין UI).
2. Product ID למוצר הססיה (מוצר קבוע, מחיר קבוע) → נערך ב-`/admin/settings`
   (`woo_session_product_id`).
3. **גילוי תשלום — אין webhook מוגדר בחנות (2026-08-23), אז המסלול הפעיל
   הוא polling**: `WOOCOMMERCE_KEY`/`WOOCOMMERCE_SECRET` (מפתחות REST API,
   **הרשאת Read בלבד מספיקה** — Settings → Advanced → REST API → Add key).
   בדיקה יזומה רצה בכל טעינת עמוד מחובר (best-effort, לא חוסמת רינדור),
   ו-cron יומי כרשת ביטחון. אם בעתיד יוגדר webhook — `WOOCOMMERCE_WEBHOOK_SECRET`
   מפעיל אותו אוטומטית, שני המסלולים עובדים במקביל בלי צורך לבחור.

---

## 3. מה זה "אדמין" במערכת

כל משתמש (מטפל או אדמין) הוא שורה בטבלת `profiles`, עם עמודה בשם `role`
שהערך שלה או `therapist` או `admin`. **אין הבדל טכני אחר** — אותה מערכת
התחברות, אותו סוג חשבון. ה-`role` הוא זה שקובע:
- אילו מסכים נגישים (`/admin/*` דורש `role='admin'`, נאכף בקוד ב-
  `lib/auth/guards.ts`).
- אילו פעולות RPC מותרות (הרבה פונקציות ב-SQL בודקות `is_admin()` בתחילתן
  וזורקות שגיאה אם לא).

**איך הופכים משתמש לאדמין:** מריצים ב-SQL Editor (יש להריץ את שתי השורות
יחד, כטרנזקציה אחת):
```sql
select set_config('baclinica.trusted_write', 'on', true);
update profiles set role = 'admin' where email = 'the-email@example.com';
```
ה-`set_config` הזה **הכרחי** — יש טריגר בטבלת `profiles` שחוסם שינוי `role`
מכל מקור שאינו אדמין מחובר או RPC מהימן, בדיוק כדי שמטפל לא יוכל להעניק
לעצמו הרשאות אדמין. בלי השורה הזו, ה-`update` ירוץ בלי שגיאה אבל **לא
ישנה כלום בפועל**.

**מה אדמין יכול שמטפל לא:** לראות את כל המטפלים ופרטיהם, לשבץ/לבטל הזמנות
של כל אחד, לאשר/לדחות בקשות ססיה, להעניק שעות מתנה, לרשום חריגות, לנהל
חדרים/סניפים/מחירים, ולערוך שעות בכרטיסיות/ססיות קיימות.

---

## 4. גישות (Access)

| שירות | חשבון | הערות |
|---|---|---|
| GitHub | `oavital77-code` | הריפו `claude-test` |
| Vercel | פרויקט **baclinica** | Environment Variables + Deployments |
| Supabase | **oavital77-code's Project** (`ekfgidxqjuysllkqdygl`) | SQL Editor, Authentication, Storage |
| Resend | חשבון שנוצר עם `oavital77@gmail.com` | שולח מיילים בפועל — עדיין ב-sandbox |
| Domain | טרם נרכש | נדרש כדי לפתור את בעיית שליחת המיילים |

---

## 5. חוק הברזל: אין כתיבה ישירה למסד הנתונים

זו לא המלצה — זו נקודה שחוזרת כמעט בכל תקלה שנפתרה בשיחה הזו. **כל פעולה
עסקית** (קביעת תור, רכישה, ביטול, אישור ססיה וכו') חייבת לעבור דרך **RPC
function** — פונקציה שכתובה ב-SQL, יושבת ב-Supabase, ומכילה את כל הבדיקות
(הרשאה, זמינות, יתרה) בתוך טרנזקציה אחת. הקוד ב-Next.js אף פעם לא כותב ישר
לטבלאות `bookings`/`punch_cards`/`session_subscriptions` — הוא רק קורא
ל-RPC (`supabase.rpc("create_booking", {...})` וכו').

**הסיבה:** 300 מטפלים מתחרים על 10 חדרים. בלי נעילה ברמת מסד הנתונים
(`for update`) וטרנזקציה אחת, שני מטפלים יכולים "לנצח" תיאורטית על אותה
משבצת בו-זמנית.

**באג אמיתי שנתפס בגלל זה:** פונקציית `create_booking` הצהירה על עמודת
פלט (`RETURNS TABLE`) בשם `hours_remaining` — בדיוק כמו עמודה אמיתית בטבלת
`punch_cards`. זה יצר התנגשות שם (PL/pgSQL variable shadowing), וגרם
לפונקציה להיכשל **בכל הזמנה, תמיד**, מאז שנכתבה — עד שאותר ותוקן. לקח: אף
פעם לא לתת לעמודת פלט RPC אותו שם כמו עמודת טבלה אמיתית, ואם כן — להכשיר
(qualify) כל שימוש בה עם שם הטבלה.

---

## 6. איך מריצים מיגרציה חדשה (שינוי במסד הנתונים)

1. הקובץ נכתב ב-`supabase/migrations/<תאריך>_<שם>.sql`.
2. **אף פעם לא עורכים קובץ מיגרציה קיים שכבר רץ** — תמיד קובץ חדש עם timestamp
   חדש, גם אם זה תיקון לקובץ קודם.
3. פותחים את הקובץ, מעתיקים **את כל התוכן** (לא את שם הקובץ!).
4. נכנסים ל-Supabase Dashboard → SQL Editor.
5. מדביקים את התוכן, לוחצים **Run**.
6. בודקים שאין שגיאה אדומה בתוצאה.

טעות נפוצה: להקליד/להדביק את **שם הקובץ** בשורת ה-SQL Editor במקום את
התוכן שבתוכו — Postgres ינסה לפרש את זה כביטוי מספרי ויזרוק שגיאת syntax.

---

## 7. מצב האימות (Authentication) הנוכחי — ולמה

**המצב המקורי** היה קוד חד-פעמי (OTP) שנשלח למייל. זה נתקל בבעיה אמיתית:
Resend (ספק המיילים) מגביל את הדומיין ברירת המחדל שלו (`onboarding@resend.dev`)
לשליחה **רק** לכתובת בעלת החשבון — לא לאף משתמש אמיתי אחר. כלומר הרשמה
עם מייל אמיתי של מטפל נכשלה תמיד.

**הפתרון הזמני שהוטמע:** מעבר להרשמה/התחברות עם **מייל + סיסמה** רגילים
(`signUp`/`signInWithPassword` של Supabase Auth), בלי תלות בשליחת מייל
בכלל. שתי הגדרות נדרשות ב-Supabase (Authentication → Sign In / Providers):
- **Allow new users to sign up** — דלוק.
- **Confirm email** — כבוי (אחרת `signUp` מחכה לאישור מייל שלא יישלח).

**מגבלה שנשארה:** אין "שכחתי סיסמה" — גם זה דורש מייל. אם מישהו ננעל,
צריך לאפס לו סיסמה ידנית דרך SQL:
```sql
update auth.users
set encrypted_password = crypt('סיסמה-חדשה', gen_salt('bf'))
where email = 'the-email@example.com';
```

**הפתרון הקבוע:** לרכוש דומיין, לאמת אותו ב-Resend (Domains → Add Domain
→ להוסיף רשומות DNS שResend נותן), ואז לעדכן את כתובת השולח גם ב-Supabase
(Authentication → Emails/SMTP) וגם ב-Vercel (`RESEND_FROM_EMAIL`). אחרי זה
אפשר לחזור לזרימת OTP אם רוצים, או פשוט להפעיל "שכחתי סיסמה" עם המצב
הנוכחי.

---

## 8. בעיות/מגבלות ידועות כרגע

| # | נושא | מצב |
|---|---|---|
| 1 | דומיין מייל מאומת | ❌ לא קיים — Resend ב-sandbox, שליחה רק לחשבון הבעלים |
| 2 | שכחתי סיסמה | ❌ לא זמין (תלוי בסעיף 1) |
| 3 | Product ID-ים ב-Woo | ⚠️ טרם הוזנו — `woo_product_tiers` ריקה (5 כרטיסיות) ו-`woo_session_product_id` = 0 (ססיה). עד שיוזנו, רכישה/תשלום ססיה נכשלים בהודעה ברורה — לא בשקט. |
| 4 | Vercel Cron | ⚠️ מוגבל לפעם ביום (תוכנית Hobby) |
| 5 | הרשאות אדמין | רק בינארי (admin/therapist) — אין תפקידים מדורגים |
| 6 | Sentry / ניטור שגיאות | ❌ לא מחובר |
| 7 | חיוב אוטומטי של חידוש ססיה חודשי | ❌ אין ואף פעם לא יהיה — בכוונה (ר' `20260828000002`). חידוש הוא תמיד פעולה יזומה של המטפל/ת (קישור טרי לחנות), או סימון ידני של אדמין (סעיף 9). |

---

## 9. תשלום ססיה במזומן — מסלול ידני חלופי

ברירת המחדל: תשלום ססיה (ראשוני וחידוש) עובר דרך חנות ה-Woo — המטפל/ת
לוחצ/ת "מעבר לתשלום"/"חידוש מנוי", משלמ/ת בחנות, וה-webhook מפעיל את
הססיה אוטומטית (אין צורך בפעולת אדמין). המסלול הידני הזה הוא **רק** למי
ששילם/ה בפועל במזומן/העברה ישירות להנהלה, לא דרך החנות:

1. **תשלום ראשון:** אחרי שמאשרים בקשת ססיה (`/admin/sessions`), התשלום
   הראשוני מופיע ב-`/admin/payments` בסטטוס "ממתין". לוחצים "סימון כשולם
   (מזומן)" — הססיה הופכת לפעילה מיד.
2. **כל חודש אחרי זה:** אין חיוב אוטומטי בכלל (גם למי ששילם בכרטיס —
   ר' סעיף 8, #7), אז אין הבדל אמיתי בין מי ששילם את התשלום הראשון
   במזומן לבין כל אחד אחר. ברירת המחדל: המטפל/ת עצמו/ה לוחצ/ת "חידוש
   מנוי" ומשלמ/ת בחנות כרגיל. אם גם החידוש משולם במזומן — יוצרים לו
   תשלום ידנית דרך ה-RPC `initiate_session_renewal_payment` (עובד גם
   לאדמין, לא רק למטפל/ת עצמו/ה — אבל **חייב** להיקרא מהדפדפן כשמחוברים
   כאדמין, למשל מ-console, ולא מה-SQL Editor, כי הפונקציה בודקת מי מחובר)
   ואז מסמנים כשולם ב-`/admin/payments` כרגיל.

---

## יומן עדכונים קריטיים

*(מהחדש לישן. כל שורה: תאריך, מה קרה, מה המשמעות התפעולית.)*

- **2026-08-23** — שוחררה לגמרי אינטגרציית PayPlus (מעולם לא חוברה בפועל,
  ר' רשומה מ-2026-08-22 למטה) לטובת תשלום דרך חנות ה-Woo לכל הסוגים —
  כרטיסייה **וגם ססיה** (ראשוני וחידוש). נמחקו `app/api/payplus/*`,
  `lib/payplus/*`, `app/api/cron/sync-payments`. **פעולה נדרשת לפני
  production:** להזין Product ID אמיתי ל-5 מוצרי הכרטיסייה (`woo_product_tiers`,
  SQL) ולמוצר הססיה (`/admin/settings` → "Product ID של מוצר הססיה בחנות
  Woo"), ולוודא שה-webhook בחנות מוגדר ומצביע ל-`/api/woo/webhook` עם
  הסוד הנכון ב-`WOOCOMMERCE_WEBHOOK_SECRET`. בלי זה, רכישה/תשלום ססיה
  נכשלים בהודעה ברורה — לא בשקט.
- **2026-08-22** — נוסף תמיכה בתשלום ססיה במזומן (סעיף 9). תוך כדי זה
  התגלה ותועד (סעיף 8, #7) ש-`chargeByToken` הוא עדיין stub — כלומר
  חיוב אוטומטי של חידוש חודשי נכשל כרגע **לכל** מנוי ססיה, לא רק למי
  ששילם במזומן, עד ש-PayPlus יאומת ויחובר (#3).
- **2026-08-22** — עבר Mobile responsiveness: שני התפריטים (מטפלים
  ואדמין) הפכו מרשימת קישורים אחת (שנשברת למובייל) לתפריט המבורגר שנפתח
  בלחיצה מתחת ל-breakpoint (`md` לתפריט המטפלים, `lg` לתפריט האדמין —
  יותר קישורים). ברירת המחדל בכל טופס/גריד חדש מעכשיו: **תמיד להתחיל
  מ-`grid-cols-1` ולהוסיף עמודות רק ב-breakpoint**, אף פעם לא `grid-cols-2`+
  בלי בסיס mobile — זו הייתה הסיבה שטפסים נדחסו במובייל. כל `<table>`
  חייב עטיפת `overflow-x-auto rounded-md border` (נמצא טופס דוחות אחד
  בלי זה ותוקן).
- **2026-08-22** — נבנה המסמך הזה. מצב האימות תועד (מייל+סיסמה זמני,
  Resend ב-sandbox). מגבלת Vercel Cron (פעם ביום) תועדה. תהליך הפיכת
  משתמש לאדמין תועד כולל ה-`trusted_write` הנדרש.
