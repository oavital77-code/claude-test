/**
 * מילת האישור שחייבים להקליד במדויק כדי לאפס את המערכת למצב אפס.
 *
 * יושבת כאן ולא ב-`app/(admin)/admin/settings/actions.ts` כי קובץ "use server"
 * יכול לייצא רק פונקציות async — ייצוא קבוע ממנו מפיל את ה-build.
 *
 * זו נוחות UI בלבד: הבדיקה האמיתית נאכפת בתוך ה-RPC `reset_system_to_zero`,
 * שמשווה את המחרוזת בעצמו. שינוי כאן בלי לשנות גם את המיגרציה רק ישבור את
 * הכפתור — הוא לא יכול לעקוף את ההגנה.
 */
export const RESET_CONFIRMATION_PHRASE = "מחק הכל";
